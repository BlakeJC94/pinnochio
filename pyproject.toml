[project]
name = "pinnochio"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "tomlkit>=0.14.0",
]

[project.scripts]
pinnochio = "pinnochio.__main__:main"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
